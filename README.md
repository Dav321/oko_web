# oko_web

### Generate CSS
```bash
npx tailwindcss -i ./input.css -o ./assets/tailwind.css --watch
```

### Running (Dev)
```bash
dx serve --platform web
```

### Docker
```bash
docker compose up -d
```